version: '3.8'

services:
  sewainaja-admin:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sewainaja-admin
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOSTNAME=0.0.0.0
      - NODE_ENV=production
